source install/setup.bash && ros2 launch moveit_client client.launch.py;

Package have code to call moveit with target end effector position.


##### Errors:

On running the moveit_client_node, you may encounter following errors:

##  No kinematics plugins defined. Fill and load kinematics.yaml

- Add launch/client.launch.Py.
- Add install(
    DIRECTORY launch
    DESTINATION share/${PROJECT_NAME})
- launch the node using ros2 launch moveit_client client.launch.py;

## Planning failed to move to the target pose!
